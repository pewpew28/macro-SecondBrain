<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/secondbrainlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/secondbrainlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".ui.note.EditNoteActivity"
            android:exported="false"
            android:label="@string/title_activity_edit_note"
            android:theme="@style/Theme.SecondBrain" />
        <activity
            android:name=".ui.capture.CaptureActivity"
            android:exported="false"
            android:label="@string/title_capture"
            android:theme="@style/Theme.SecondBrain" />
        <activity
            android:name=".ui.note.OpenNoteActivity"
            android:exported="false"
            android:label="@string/title_note"
            android:theme="@style/Theme.SecondBrain" />
        <activity
            android:name=".ui.note.FolderActivity"
            android:exported="false" />
        <activity
            android:name=".LandingPage"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SecondBrain" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".splash_screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>